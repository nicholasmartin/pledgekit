/**
 * API Request Types
 * 
 * This file contains type definitions for API request payloads.
 * Each request type should be prefixed with the HTTP method it supports.
 */

import { z } from 'zod'

// Example: Will be populated with actual request types
export type GetProjectRequest = {
  id: string
}
